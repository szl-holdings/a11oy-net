name: Holographic Proof v2 Gate and Merge

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  actions: read
  checks: read
  contents: write
  pull-requests: write
  statuses: read

concurrency:
  group: holographic-proof-v2-gate-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  gate-and-merge:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.head_ref == 'feat/holographic-proof-v2-20260902' &&
      github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      GH_TOKEN: ${{ github.token }}
      HEAD_SHA: ${{ github.event.pull_request.head.sha }}
      PR_NUMBER: ${{ github.event.pull_request.number }}
      SELF_RUN_ID: ${{ github.run_id }}
    steps:
      - name: Checkout exact candidate
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ env.HEAD_SHA }}
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Run exact local proof contracts
        run: |
          python -m py_compile tools/rollout_holographic_proof_v2.py tests/test_holographic_proof_v2.py
          python tests/test_holographic_proof_v2.py
          git diff --check
      - name: Wait for every peer check to finish green
        shell: bash
        run: |
          python - <<'PY'
          import json
          import os
          import subprocess
          import time

          repo = os.environ["GITHUB_REPOSITORY"]
          sha = os.environ["HEAD_SHA"]
          self_run = f"/actions/runs/{os.environ['SELF_RUN_ID']}"
          allowed = {"success", "neutral", "skipped"}

          def api(path):
              return json.loads(subprocess.check_output(["gh", "api", path], text=True))

          for attempt in range(180):
              checks = api(f"repos/{repo}/commits/{sha}/check-runs?per_page=100").get("check_runs", [])
              peers = [row for row in checks if self_run not in str(row.get("details_url") or "")]
              bad = [row for row in peers if row.get("status") == "completed" and row.get("conclusion") not in allowed]
              if bad:
                  compact = [{"name": row.get("name"), "conclusion": row.get("conclusion"), "url": row.get("html_url")} for row in bad]
                  raise SystemExit("peer check failure: " + json.dumps(compact, sort_keys=True))
              pending = [row for row in peers if row.get("status") != "completed"]
              status = api(f"repos/{repo}/commits/{sha}/status")
              bad_status = [row for row in status.get("statuses", []) if row.get("state") in {"error", "failure"}]
              if bad_status:
                  raise SystemExit("legacy status failure: " + json.dumps(bad_status, sort_keys=True)[:4000])
              if peers and not pending and status.get("state") != "pending":
                  print(f"All {len(peers)} peer checks completed green.")
                  break
              time.sleep(10)
          else:
              raise SystemExit("timed out waiting for peer checks")
          PY
      - name: Squash merge without bypassing protections
        run: |
          gh pr merge "$PR_NUMBER" \
            --repo "$GITHUB_REPOSITORY" \
            --squash \
            --delete-branch \
            --match-head-commit "$HEAD_SHA" \
            --subject "feat(frontend): ship SZL Holographic Proof v2 (#$PR_NUMBER)" \
            --body "Original CSS-only holographic evidence plane, distinct record textures, zero-JavaScript preservation, guarded source rollout, accessibility behavior, and offline contracts.\n\nSigned-off-by: Stephen Lutar <stephenlutar2@gmail.com>"
